import React from "react";

const WalletButton: React.FC = () => {
  return (
    <button className="bg-primary hover:bg-secondary text-white py-2 px-4 rounded transition duration-300">
      Wallet
    </button>
  );
};

export default WalletButton;
